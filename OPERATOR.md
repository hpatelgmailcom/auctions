# OPERATOR

This file contains notes from the operator.

## Backlog

- [] **Add more providers** — We need add to more data providers. In orders to do this you'll have to build to read gmail message via api. The messages will not be fed into AI. But they will be parsed. So you can use AI on few samples from each sender, one time onlt, and generate parsing utility for them. After that, the you/agent should not use use AI to read email. The scope will be get email messages via API, send it to correct parser. For now use the PROVIDERS.md to view list of providers. In future, you'll be adding more providers based on your internet searches. 


